# Firebase Configuration
# Get these values from your Firebase project settings at https://console.firebase.google.com
# Project Settings > General > Your apps > Web app > Firebase SDK snippet > Config

NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Optional: Firebase Analytics Measurement ID
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id

# Firebase Admin SDK (Server-side)
# Get this from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Paste the entire JSON content as a single line (stringify it)
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your_project",...}

# Google AI (Gemini) Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=your_gemini_api_key_here

# AI privacy controls
# Default should remain false in production to avoid sending direct identifiers.
AI_ALLOW_PII=false

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual Firebase and Google AI credentials
# 3. Never commit .env.local to version control (it's already in .gitignore)
